[package]
name = "armv8-baremetal-demo-rust"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
spin = "0.7"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"